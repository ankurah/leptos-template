# Rust
/target/
**/*.rs.bk
Cargo.lock

# Trunk / WASM build output
leptos-app/dist/
*.wasm

# Local config: ignore only the akdev-managed root patch. Nested .cargo dirs
# (e.g. leptos-app/.cargo/config.toml, which carries wasm build flags) are kept.
/.cargo/

# e2e (Playwright)
e2e/node_modules/
e2e/test-results/
e2e/package-lock.json

# Dev runner (local, per-checkout state)
.dev-ports
.dev-pid
.dev-log

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
